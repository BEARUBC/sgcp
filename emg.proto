syntax = "proto3";

package sgcp.emg;

enum Task {
    UNDEFINED_TASK = 0;
    CALIBRATE = 1; 
    IDLE = 2;
    ABORT = 3;
    // @todo krarpit implement EMG resource management and relevant task definitions
}

message TaskData {}